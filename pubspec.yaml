name: helloworld
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_auth:
  cloud_firestore:
  flutter_svg: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
